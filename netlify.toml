[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Optional: disable automatic framework detection / Netlify plugins at repo level.
# If your Netlify site uses a plugin (e.g. "neon") and fails with ENETUNREACH,
# remove that plugin from the site settings or lock it to a reachable package version.
